/// Handle Move source maps for mapping bytecode offsets to source locations.
///
/// This is a stub that will be implemented in M2 once
/// move-bytecode-source-map dependencies are available.

pub struct SourceMapResolver;

impl SourceMapResolver {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SourceMapResolver {
    fn default() -> Self {
        Self::new()
    }
}
