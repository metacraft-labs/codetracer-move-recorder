/// Convert Move types to CodeTracer trace types.
///
/// This is a stub that will be implemented in M2 once
/// move-trace-format dependencies are available.

pub struct MoveTypeConverter;

impl MoveTypeConverter {
    pub fn new() -> Self {
        Self
    }
}

impl Default for MoveTypeConverter {
    fn default() -> Self {
        Self::new()
    }
}
